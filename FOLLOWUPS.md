# Follow-ups

Running list of things noted along the way that don't need to block current work, to revisit later.

- ~~PDF download/print was broken (blank, mismatched, or capturing the wrong screen).~~ Resolved 2026-09-03 for real this time: after three attempts at fixing this via the browser's print pipeline (a CSS visibility trick, a full-page DOM swap, then an isolated iframe) all still ended with iOS Safari capturing the wrong on-screen content, switched approach entirely — "Download PDF" now generates a real PDF file client-side with the `pdfmake` library and hands it straight to the browser as a download, never touching `window.print()`/the browser's print dialog at all. Applies to both QuickQuote and Invoices.
  - ~~Known gap from this fix~~: Resolved 2026-09-04 — invoice PDFs now embed job condition photos too, in a "Job Condition Photos" section (3 per row) between the condition notes and the itemized services. Photos are fetched from Storage and converted to embedded data URIs the same way the business logo already was; a photo that fails to fetch is just dropped rather than failing the whole PDF.

- ~~Set up git.~~ Resolved 2026-08-25: installed Git for Windows via winget, ran `git init`, and made the first commit (`d92f0e4`, 30 files). `.gitignore` also now excludes `dev.log` in addition to `node_modules`/`dist`/`.env*`.
- ~~Re-send corrected quote to Andre Green.~~ Resolved 2026-08-25: quote `7a78190c-acd1-4388-91a5-73f5683e3bfc`'s saved tiers were corrected in the database (Essential $187.25, Signature $642.00, Premium $856.00). User confirmed no need to actually resend it to the customer.
- **Whole-app bug audit (2026-08-25).** Ran a full correctness review of every page and fixed everything high/medium severity that was found: Invoices (Overdue status now auto-computes from due date instead of staying frozen forever, itemizing services now requires picking a vehicle so pricing can't silently default to car pricing on an SUV/truck/van job, fixed a sign error that was hiding the "days overdue" count, "Marked paid" now records and shows the real payment date via a new `paid_at` column, and invoices now snapshot service name/price at save time via a new `line_items` column so a later-deleted or renamed service can't corrupt an already-issued invoice's PDF), Schedule ("Week revenue" was silently dropping jobs near the week boundary due to a time-of-day bug, and the new-job price hint now respects the selected vehicle's size class instead of always showing car pricing), Dashboard ("Today's Revenue"/"Today's Profit"/"Earnings this week" now only count completed jobs, not merely scheduled ones), Settings (removed the "high" price and "Deposit" fields from the Services editor since they saved but were never used anywhere else in the app), Customers ("Call"/"Text" buttons are now real `tel:`/`sms:` links instead of doing nothing), and Auth (a business-lookup failure no longer hangs on an infinite spinner with no error message, unchecking "keep me signed in" no longer logs you out on a plain page refresh, and a timing race that could flash real dashboard data before a password-reset screen was closed). All verified working in the browser.
- ~~Dashboard: "Pipeline Value" summed the *most expensive* option on every open tiered quote, not a more conservative expected value.~~ Resolved 2026-09-04, flagging it explicitly since it changes a number you look at: the Pipeline Value KPI now values each tiered quote at the **midpoint** of its price range rather than the top tier, so the total reads as a realistic estimate instead of a best case (a tile tooltip on hover explains this). A single quote's own displayed amount elsewhere in the app (e.g. the Recent Quotes list) is unchanged — it still shows "up to $X", which is accurate for one quote on its own; only the *sum across every open quote* used the more conservative number, since always taking the max compounds across many quotes into something that reads misleadingly optimistic.
- ~~Invoices: manually-typed amount treated as tax-inclusive.~~ Resolved 2026-08-25: the "Amount" field for a non-itemized invoice is now labeled "Price ($, before tax)" and tax is added on top at submit time — same model as itemized services — with a live "+X% tax = $Y total" preview so it's clear what the customer's actually charged. Linking a quote or editing an existing manual invoice correctly converts between the stored tax-inclusive total and the pre-tax price shown in the field.
- **NEXT FEATURE: Automated customer texting tied to Schedule.** Confirmed 2026-08-25 that this doesn't exist yet — quotes/invoices only have manual `tel:`/`sms:` links and a "What to say" script the owner sends by hand; Settings → Integrations lists Twilio/WhatsApp as options but nothing is actually connected. User wants, tied to Schedule: an automatic text when an appointment is booked, a reminder before the appointment, and a post-job text with a review request once the job's marked completed. This needs a real texting provider (Twilio is standard, costs a small amount per text) connected via a Supabase Edge Function triggered off job status/scheduled time changes.
  - **Step 1 shipped 2026-08-25**: booking-confirmation piece only. Creating a job in Schedule now shows a "here's what the customer gets" preview (editable text, filled from customer/vehicle/date/time), a Copy button, and a manual "Text this to {customer}" `sms:` link. An "Send automatically" toggle is visible but intentionally inert — labeled "needs a texting service connected first" — since there's no Twilio/etc. wired up yet.
  - **Still to build**: the actual auto-send (needs Twilio account + a Supabase Edge Function), the day-before reminder text, the post-completion review-request text, and (added 2026-08-25) an "on my way"/en-route text tied to the job's address.
- **Mobile responsiveness — real gap, not covered by the correctness audit.** The 2026-08-25 whole-app audit checked logic/math/data correctness, not phone layout. User found real issues testing on an iPhone (added to home screen): Dashboard's stat-card grid was rigidly 4 columns at every width, causing label text to wrap and clip into icon badges on narrow screens (fixed — now 2 columns under 640px); the "New Job"/"New Quote" quick-actions collapsed to two identical unlabeled "+" icons on mobile (fixed — distinct icons + title/aria-label). Followed up with a full sweep of every page's layout code for the same "rigid grid column count, no breakpoint" pattern: fixed Settings' paired form fields (Business Profile, Online Booking) and QuickQuote's tiered-pricing comparison + discount/tax fields, all now stacking to one column under 640px. Auto-fit/auto-fill grids (stat cards, photo galleries) across every page were already self-responsive and didn't need changes; Schedule's calendar grids need exactly 7 columns and already use the overflow-safe pattern, so those were left alone; print-only layouts (PrintableInvoice/PrintableQuote) never render on screen so don't matter for mobile. **What's still unverified**: none of this has been checked live on an actual phone yet (my browser tools couldn't reliably emulate a phone viewport this session) — the grep-based sweep found and fixed every instance of the specific bug pattern that caused the original complaint, but a real walkthrough on an iPhone/Android/iPad to catch anything a code search would miss (overflow from other causes, touch-target sizing, etc.) still needs to happen.
- **Netlify's own "Powered by Netlify" badge overlaps the mobile bottom nav bar**, actively blocking the "Customers" tab on a phone. This isn't something to fix in app code. Confirmed 2026-08-25 via Netlify's own docs: there's a real free-plan toggle at **Project configuration → General → "Powered by Netlify badge"** (badge defaults on for free-plan sites created on/after Aug 19, 2026) — a CSS-hack workaround was considered and rejected in favor of this official setting. Not yet confirmed the user actually found/used the toggle.
- ~~Dashboard KPI tiles weren't clickable.~~ Resolved 2026-08-25: Today's Revenue/Jobs → Schedule, Customers → Customers, Pipeline Value → QuickQuote, Outstanding/Avg. Ticket → Invoices. Whole tile is clickable (icon included), no separate option/toggle added since there's no real downside to just having it on.
- ~~No way to edit a scheduled job, and clicking a job block in the month calendar did nothing.~~ Resolved 2026-08-25: the month view's job chips had an `onClick` that existed only to `stopPropagation()` (so drag-to-reschedule wouldn't also trigger the day cell's preview toggle) with no actual action — that's exactly why clicking the block did nothing while clicking around it worked. AddJobModal now handles edit as well as create (pre-fills from the job, saves as an UPDATE, adds a delete button); job rows in month/day/day-preview views now open it on click. **Follow-up fix same day**: removing that onClick wasn't quite enough on touch devices — chips were still marked `draggable`, and WebKit can treat a tap on a draggable element as the start of a drag gesture rather than a plain tap even though native HTML5 drag doesn't actually work via touch. Chips are now only draggable on fine-pointer (mouse) devices, so every tap in a day cell reliably opens that day on phones.
- ~~QuickQuote's "New vehicle" button abandoned the in-progress quote.~~ Resolved 2026-08-25: same root issue as the Schedule one above — it called `onNavigate("vehicles")`, which unmounts QuickQuote entirely (customer pick, step position, everything lost). Now an inline add-vehicle form matching Schedule's, auto-selecting the new vehicle into the quote.
- ~~Logo picker's camera badge and business-name pencil were permanent decorations.~~ Resolved 2026-08-25: camera badge (bottom-right of the logo circle) now only shows before a logo's been uploaded — tapping the logo image itself still opens the file picker regardless. Pencil icon next to the business name removed everywhere (tapping the name text still opens the rename field). Fixed across all 8 files with their own copy of `BrandLockup`.
- ~~Customer address + one-tap directions.~~ Shipped 2026-08-25: `address` field added to customers (on the Add Customer form, and inline-editable in the customer detail drawer), a "Directions" quick-action next to Call/Text, and a Directions icon on job rows in Schedule's day view/day-preview that opens Google Maps turn-by-turn (works with either Google or Apple Maps installed). The "en route" notification half of this request is folded into the not-yet-built automated-texting feature below, since it needs the same Twilio/texting infrastructure.
- ~~Possible feature: autosave/draft-recovery for in-progress forms.~~ Resolved 2026-09-04: added a shared `useDraftAutosave`/`readDraft`/`clearDraft` helper (`lib.js`) that periodically snapshots an in-progress form to localStorage, wired into Schedule's New Job form and Invoices' New Invoice form (edits are skipped — they already have real, saved data). A restored draft shows a small "Restored an unsaved X from earlier / Discard" banner; it clears on a successful save or an explicit close. Invoice photos are excluded from the snapshot — too large for localStorage and the lower-stakes part of an invoice. QuickQuote wasn't touched since it already has a manual "Save draft."
- **Print/PDF "this site has been blocked from opening dialogs" message.** Not really an app bug — Chrome throttles `window.print()`/dialog calls it judges too disconnected from a direct click, especially after several in a short span (which happened during this session's own testing). Tightened the code anyway (removed an unnecessary `setTimeout` hop between the click and the print call in both Invoices and QuickQuote) so future calls stay as close to the user gesture as possible. If it recurs on a fresh tab under normal (non-rapid) use, that would be worth a closer look; otherwise closing/reopening the tab clears it.
- ~~PDF/print came out completely blank on iPhone.~~ Resolved 2026-09-03: found the real bug behind this one. The print stylesheet used the common `body * { visibility: hidden }` + `position: absolute` trick to show only the printable quote/invoice — that specific trick is known to render a blank page in iOS Safari's print pipeline. Replaced it in both QuickQuote and Invoices with a `.atlas-screen-only { display: none }` approach at print time, which is the reliable, WebKit-safe way to do print-only content. Verified via `npm run build`; still needs a real print/PDF test on an iPhone to fully confirm.
- ~~Standalone Vehicles page had no way to edit a vehicle after creation.~~ Resolved 2026-09-03: vehicle cards are clickable now and open the same Add Vehicle modal in edit mode (pre-filled, "Save changes", delete button) — same pattern as job editing in Schedule. (Editing a vehicle from inside a customer's profile drawer was already possible from an earlier fix this session.)
- ~~Customer profile had no service history, quotes, or amount spent.~~ Resolved 2026-09-03: customer detail now shows a "Total spent" figure (sum of paid invoices), a completed-jobs count, a Service history list, and a Quotes list — each tapping through to Schedule or QuickQuote.
- ~~Settings → Services: no way to duplicate a service, and descriptions had to be bullet points.~~ Resolved 2026-09-03: added a Duplicate button per service (clones it with "(copy)" appended) and a plain-text Description field above the bullets (new `services.description` column) — also now prints on the customer-facing quote PDF.
- **Delivery confirmation for sent texts/emails** — user asked whether there's a way to see if a text/email actually went through. Not built yet: this needs real send-status tracking (a texting/email provider like Twilio/Resend with delivery webhooks), which is the same infrastructure gap as the not-yet-built automated-texting feature above. Folding this into that future work rather than building a separate one-off.
- **Facebook/Meta Ads lead integration — receiving side deployed 2026-09-04, waiting on your Meta-side setup.** User is currently getting leads via Facebook Messenger and Lead Ads forms and wants them landing in Atlas as customers automatically. Deployed a Supabase Edge Function, `facebook-lead-webhook` (source also checked into `supabase/functions/facebook-lead-webhook/index.ts`), that receives Meta's Lead Ads webhook, fetches the real name/email/phone via the Graph API, and creates a customer tagged `source: facebook_lead_ads` (deduped by `source_ref` = the lead's id, so Meta's webhook retries can't create duplicates). It does nothing until you complete the Meta-side setup — **this part needs your Facebook/Meta account, not something that can be done from here:**
  1. **Function URL** (you'll enter this in Meta): `https://fqggnekepfzxrkuazpiy.supabase.co/functions/v1/facebook-lead-webhook`
  2. In [Meta for Developers](https://developers.facebook.com/), create an App (or use an existing one) → add the **Webhooks** product → subscribe to **Page** → field **leadgen** → callback URL is the function URL above, verify token is whatever string you pick (suggested: `136b2ce67a19ab80729dd7cfa8f2e5deb6bf26f0e5e9f338`).
  3. Subscribe your actual Facebook Page to that app's leadgen webhook (Meta's docs call this out as a separate step from the App-level subscription above).
  4. Get a **Page Access Token** for that Page with the `leads_retrieval` permission (Graph API Explorer, or your app's Page settings).
  5. Get the App Secret from the App's Settings → Basic.
  6. In the Supabase dashboard → this project → Edge Functions → `facebook-lead-webhook` → Secrets, set: `FB_VERIFY_TOKEN` (same value as step 2), `FB_APP_SECRET` (step 5), `FB_PAGE_ACCESS_TOKEN` (step 4). `ATLAS_BUSINESS_ID` is already set to Detail Hero's id.
  - While the Meta App is in Development mode, leads only flow for people with a role on the app (you); going live for real customers' ad campaigns needs Meta's App Review for the Leads Retrieval permission, same as before.
  - **Progress checkpoint (2026-09-04, in progress)**: Meta App "Atlas Lead Integration" created under the DetailHero.Net business portfolio, with the "Capture & manage ad leads with Marketing API" use case attached. App Secret has been retrieved (not written here — it's a live credential; only the App owner has it, needs entering directly into Supabase's Edge Functions → Secrets UI, which this session has no API access to do remotely). **Still to do**: set `FB_APP_SECRET` and `FB_VERIFY_TOKEN` in Supabase, then set up the actual Webhooks subscription in the Meta App (leadgen field → the function URL above), subscribe the Page to it, and get the Page Access Token for `FB_PAGE_ACCESS_TOKEN`.
  - **Not built**: a push/text notification to the owner when a lead comes in (needs the same Twilio infrastructure as the not-yet-built automated-texting feature), and Messenger-conversation leads — those are unstructured chat, not a form submission, so auto-capturing them as customers is a materially bigger, separate integration (the Messenger Platform's webhook + a way to decide what counts as "a lead" from a conversation). A fast manual "Add lead" shortcut tagged by source could bridge that gap in the meantime if useful.
- ~~No way to delete a customer record.~~ Resolved 2026-09-04: added a "Delete customer" button to the customer detail drawer, with a confirmation that spells out the consequences (checked against the actual DB foreign-key rules first, via the Supabase MCP): their vehicles are deleted along with them (that FK cascades), but any existing jobs, quotes, and invoices are kept for the business's records — they just lose the customer link and show as "No customer" going forward, matching how the app already displays jobs/quotes/invoices with no owner elsewhere. (Editing a customer's name/phone/email/address in place already existed from an earlier fix this session and needed no further work.)
- ~~Customers: bulk-select checkboxes existed with no action wired up behind them.~~ Resolved 2026-09-04: selecting one or more customers now shows an action bar above the list ("N selected · Export selected · Delete selected · Clear"). Export selected reuses the existing CSV export scoped to the selection; Delete selected reuses the same delete call and confirmation logic as the new single-customer delete, batched via `.in("id", …)`. Bulk messaging wasn't built — sending a text to multiple recipients at once isn't reliably doable via a plain `sms:` link across phones, and it's the same infrastructure gap as the not-yet-built automated-texting feature above.
- ~~Schedule: no double-booking/overlap detection.~~ Resolved 2026-09-04: there's no per-service duration data to work from, so overlap is approximated as another active (non-cancelled) job within 60 minutes of the proposed time. It only warns — an amber banner in the add/edit form, a confirm prompt on drag-to-reschedule — rather than blocking the save, since a two-person crew can legitimately run two jobs at once.
- ~~Settings: Business Profile's phone/email/address fields were placeholder-only and never saved.~~ Resolved 2026-09-04: added real `phone`/`email`/`address` columns to `businesses`, wired them through `useBusinessId` and the same per-field Save pattern as the other Business Profile fields, and now print them on the quote/invoice PDF headers and on-screen previews too, matching what the panel already told users it did ("this is what customers see on quotes, invoices, and emails").
- ~~Add Customer only had Name/Email/Phone/Address — no way to add a vehicle or notes for a new customer without leaving the form.~~ Resolved 2026-09-04: Add Customer now has a Notes field (new `customers.notes` column, also editable later from the customer detail drawer next to Phone/Email/Address) and an optional inline Vehicle section (description + type + size class) that files the vehicle under the new customer right after it's created — same fields as the "Add vehicle" flow elsewhere. A vehicle-save failure doesn't block the customer from being added; it's reported and the vehicle can be added afterward from the customer's detail drawer.
